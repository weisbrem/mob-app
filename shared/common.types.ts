export type TFetchStatus = 'idle' | 'pending' | 'fulfilled' | 'rejected';

export const AppRoutes = {
  root: '/',
  auth: '/auth/login',
  login: '/login',
  restore: '/restore',
  myCourses: '/my-courses',
};
